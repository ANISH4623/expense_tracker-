"use client"
import { cookies } from "next/headers";

export default function Page() {
    
    return (
        <div>
            <h1></h1>
            <button  >Logout</button>
        </div>
    );
}